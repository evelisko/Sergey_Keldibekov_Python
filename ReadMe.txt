FeedBack
----------------------------------------------------------------------
" def my_func(self, a, b):
return a + b" - зачем?
" for ex, ey in map(lambda *x: x, self.matrix, other.matrix):
result.append(list(map(lambda x, y: x + y, ex, ey)))" - мощно написали
во 2 дз идею уловили
3 тоже верно