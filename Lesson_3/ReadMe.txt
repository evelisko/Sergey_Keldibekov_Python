Практическое задание

  1) Практически все задачи решены двумя способами. один как требовалось,
    другой как мне больше понравилось, как  красиво.
    Все-таки, встроенные механизмы тоже надо осваивать.

  2) Коментарии к коду и вопросы по программе сделал прямо в коде.
    мне так кажется удобнее.
    Если считаете что это плохая идея. В дальнейшем буду все в ReadMe выкладывать.

  3) Немного не понял, как передавать правильно в функцию аргументы определнного типа.
    Иногда это может потребоваться.
    К примеру -  в 5-м заднии, в строке summa += return_data(digit_array) у меня pCharm подсвечиват.
    Это (подсветку - ошибку) можно обойти, если передать в функцию строку целеком. и разбор производить уже внутри,
    но хотелось бы во всем разобраться.
	
feedBack. 
---------------------------------------------------------------------------------------------
"def print_user_dat_xe(**kwargs):" - гораздо лучше 1 варианта
в 4 дз лучше range() использовать
"# незнал как явно указать, что функция должна принимать массив строк" - есть в питоне аннотации, но это не нужно делать, язык с динамической типизацией. точка.
"tmp = ord(string_data[0]) - 32" - отлично, что догадались